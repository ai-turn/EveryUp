services:
  mt-log-agent:
    build: .
    image: aiturn/everyup-log-agent:latest
    restart: unless-stopped
    env_file:
      - path: .env
        required: true
    volumes:
      - ${LOG_AGENT_PATH:-/var/log/app}:/var/log/app
